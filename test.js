//Temperature Converter: Create a function that converts temperature between Celsius and Fahrenheit.
//define the function an dits name
//define the parfamerers of the function
//use an if statement to toggle throught the conditions
//using a formula calculate the values of each step
//retrun the value
/* function temperatureConverter(temperature, unit) {
  if (unit === "Celcius") {
    return (temperature * 9) / 5 + 32; //calculated the celsius into a farenheit
  } else if (unit === "farenheit") {
    return ((temperature - 32) * 5) / 9; //calculate farenheit to celcius
  } else {
    return " Please Input a valid unit Either Celcius Or Farenheit";
  }
}

let myTempareture = 103;
let myUnit = "farenheit";

console.log(temperatureConverter(myTempareture, myUnit)); */
//call the function and print the

//Write a function that calculates the area of a rectangle, given its length and width.
//function name
//declares parameters (length,width)\
//calculate the area using the formula(l*w)
//used an arrow function
let answer=(length,width)=>{
    return length*width;
}
console.log(answer(20,10));